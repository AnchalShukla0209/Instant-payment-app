export interface AdminFeature {
  key: string;
  label: string;
  icon: string;
  isEnabled: boolean;
  providerCode: string;
}


export interface AdminProvider {
  key: string;
  label: string;
  isEnabled: boolean;
  icon: string;
}
